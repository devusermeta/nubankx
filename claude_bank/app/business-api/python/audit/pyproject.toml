[project]
name = "banking-assistant-audit-mcp"
version = "0.1.0"
description = "Python MCP server for audit/decision ledger using fastmcp"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastmcp",
    "pydantic",
    "filelock"
]
