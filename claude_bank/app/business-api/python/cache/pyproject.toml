[project]
name = "banking-assistant-cache-mcp"
version = "0.1.0"
description = "Python MCP server for cache management using fastmcp"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastmcp",
    "pydantic",
    "filelock"
]
