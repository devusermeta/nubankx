[project]
name = "banking-assistant-limits-mcp"
version = "0.1.0"
description = "Python MCP server for limits/policy gate using fastmcp"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastmcp",
    "pydantic",
    "filelock"
]
